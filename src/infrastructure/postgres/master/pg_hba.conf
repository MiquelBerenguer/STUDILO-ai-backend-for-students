# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 1. Local y Localhost
local   all             all                                     trust
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# 2. REPLICACIÓN (Para HA) - Red interna Docker
host    replication     replicator      0.0.0.0/0               trust

# 3. HAPROXY CHECK (Para HA)
host    all             haproxy_check   0.0.0.0/0               trust

# 4. APLICACIÓN (app_user)
host    tutor_ia_db     app_user        0.0.0.0/0               scram-sha-256

# 5. [MISSING RULE] ADMINISTRADOR / DEV (tutor_user)
# Esta es la línea que faltaba y causaba el error ❌
host    all             tutor_user      0.0.0.0/0               scram-sha-256